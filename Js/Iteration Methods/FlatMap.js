// iterates over each element in an array 
///  it will create a new array

const myArr = [1, 2, 3, 4, 5, 6];
const newArr = myArr.flatMap((x) => x * 2);
console.log(newArr);

// same like map
// doubts und .. map and flat map